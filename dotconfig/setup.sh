#!/bin/sh

# make sure alacritty and nvim actually need the extra dir layer
ln -s ~/dotfiles/dotconfig/alacritty ~/.config
ln -s ~/dotfiles/dotconfig/nvim ~/.config
ln -s ~/dotfiles/dotconfig/helix ~/.config

